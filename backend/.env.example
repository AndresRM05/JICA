# Configuración local para el MVP de presentación.
# El backend corre con datos en memoria, por lo que no requiere PostgreSQL para la demo.

PORT=3000
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
