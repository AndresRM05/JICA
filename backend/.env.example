# Copia este archivo a .env y ajusta la URL de PostgreSQL local.
# No incluyas credenciales reales en el repositorio.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/jica_local?schema=public"
